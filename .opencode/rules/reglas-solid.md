# Reglas SOLID para generar código Python

> Nota de migración: esta regla es **global** (fuente única:
> `~/.config/opencode/rules/solid-python.md`, cargada vía `instructions` del config
> global de opencode). Este archivo se conserva por compatibilidad con las
> referencias locales del repo. No editar acá: los cambios van al global.