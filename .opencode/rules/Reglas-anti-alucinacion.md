# Reglas de verificación y flujo controlado

> Nota de migración: esta regla es **global** (fuente única:
> `~/.config/opencode/rules/anti-alucinacion.md`, cargada vía `instructions` del
> config global de opencode). Este archivo se conserva por compatibilidad con las
> referencias locales del repo. No editar acá: los cambios van al global.